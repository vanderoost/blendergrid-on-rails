default: &defaults
  env: <%= Rails.env %>
  bucket: swarm-engine-dev
  key_prefix: uploads

test:
  <<: *defaults

development:
  <<: *defaults
  env: dev

production:
  <<: *defaults
  env: staging # ENV of the Swarm Engine - Production is reserved for the live site
  bucket: blendergrid-staging-us-east-1
