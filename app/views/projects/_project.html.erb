<% full = local_assigns.fetch(:full, false) %>

<%= tag.div id: dom_id(project) do %>
  <% if full %>
    <h1><%= project.blend_filepath %></h1>
  <% else %>
    <h2><%= link_to(project.blend_filepath, project) %></h2>
  <% end %>

  <p>
    <%= "Project Status: #{project.status}" %>
  </p>

  <% if project.benchmarked? %>
    <%= "Price: #{number_to_currency(project.price_cents&.fdiv(100))}" %>
  <% end %>

  <%= render partial: "renders/render", object: project.render if project.render.present? %>

<% end %>
