<%= tag.div id: dom_id(quote) do %>
  <%= "Price Calculation Status: #{quote.status}" %>
  <%= "Node Type: #{quote.node_type_name}" %>
  <%= "Price: $#{quote.price}" %>

  <% unless quote.timing.blank? %>
    <%= debug quote.timing %>
  <% end %>
<% end %>
