default: &defaults
  env: <%= Rails.env %>
  bucket: swarm-engine-dev
  key_prefix: uploads

test:
  <<: *defaults

development:
  <<: *defaults

production:
  <<: *defaults
  env: prod
  bucket: blendergrid-staging-us-east-1
