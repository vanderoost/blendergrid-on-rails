default: &defaults
  env: <%= Rails.env %>
  bucket: swarm-engine-dev
  key_prefix: uploads

test:
  <<: *defaults

development:
  <<: *defaults
  env: dev

production:
  <<: *defaults
  env: staging
  bucket: blendergrid-staging-us-east-1
