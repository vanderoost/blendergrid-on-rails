<% if @projects_by_stage.empty? %>
  <h2>Get started by creating a new project.<h2>
<% else %>

  <% Project::STAGES.each do |stage| %>
    <% projects = @projects_by_stage.fetch(stage, nil)
    next if !projects %>
      <%= render projects %>
  <% end %>
<% end %>
